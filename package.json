{
  "name": "sellnautix-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && rm -rf dist && mkdir -p dist/server dist/.openai && cp .openai/hosting.json dist/.openai/hosting.json && node scripts/create-edge-bundle.mjs",
    "start": "vite preview --host 0.0.0.0",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest"
  }
}
