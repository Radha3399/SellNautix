{
  "name": "sellnautix-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && rm -rf dist && mkdir -p dist/server dist/.openai && cp .openai/hosting.json dist/.openai/hosting.json && node scripts/create-edge-bundle.mjs",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
